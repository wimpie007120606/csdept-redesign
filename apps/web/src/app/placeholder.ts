/** Default profile image used when a staff/research photo is missing or fails to load. */
export const DEFAULT_PROFILE_IMAGE = '/brand/stellenbosch/su-logo-primary.jpeg';

/** Backwards-compatible alias for existing imports. */
export const PLACEHOLDER_IMAGE = DEFAULT_PROFILE_IMAGE;
