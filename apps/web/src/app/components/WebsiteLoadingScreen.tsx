/**
 * Loading screen is implemented as HTML preloader in index.html (Mac animation + fade-out).
 * This component is disabled to avoid duplicate loaders.
 */
export const WEBSITE_LOADING_DISPLAY_MS = 3000;
export const WEBSITE_LOADING_FADEOUT_MS = 400;

export function WebsiteLoadingScreen() {
  return null;
}
